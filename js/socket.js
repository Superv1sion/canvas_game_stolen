/**
 * Created by vision on 3/31/17.
 */
var allItems = [];
var socket;
$(document).ready(function(){
    // Connect to our node/websockets server
    socket = io.connect('http://localhost:3000/');

    // Initial set of notes, loop through and add to list



})